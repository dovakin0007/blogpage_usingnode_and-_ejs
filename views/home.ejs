<%- include("partials/header.ejs") -%>
    <h1>
        Home
    </h1>

    <p>
        <%=firstcontent %>
    </p>

    <%posts.forEach(function(post){ %>
        <h1>
            <%= post.title %>
        </h1>
        <p>
            <%= post.message.substring(0, 100)+"....."%><a href="/post/<%=post._id%>" )>Read More</a>
        </p>
        <% }); %>
            <%- include("partials/footer.ejs") -%>